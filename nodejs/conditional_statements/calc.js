"use strict";
const prompt = require('prompt-sync')();
let valu1 = parseInt(prompt("Enter Value: "));
let valu2 = parseInt(prompt("Enter Vlue: "));
let wish = parseInt(promtp("What action you want: "));
let result;
if (wish == +) {
    result = (value1 - value2);
}
else {
    console.log("Enter Valid Expression: ");
}
console.log(result);
